# Nixpacks configuration for Railway
# Use with Root Directory: services/costs

[phases.setup]
nixPkgs = ['nodejs-18_x', 'postgresql']

[phases.install]
cmds = ['npm install -g pnpm@8', 'pnpm install --frozen-lockfile']

[phases.build]
cmds = ['pnpm run build']

[start]
cmd = 'pnpm run start:prod'
